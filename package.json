{
  "name": "pi-agent-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Image 2: PI agent server. HTTP+SSE façade in front of pi-sdk-runtime. Emits the same SSE event vocabulary the existing gateway already understands.",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "elysia": "^1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
